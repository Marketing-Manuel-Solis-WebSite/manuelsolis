# SEO Health Audit — 2026-09-01

**Site:** manuelsolis.com (Next.js 16 on Vercel, Manuel Solis Law Firm)
**Audit type:** Biweekly automated — source-code inspection
**Overall status:** 🟢 GREEN (with one advisory note on today's colaboradores change)

> **⚠️ Scope limitation:** The remote execution environment's egress proxy blocked outbound HTTPS to `www.manuelsolis.com` (policy denial — HTTP 403 on CONNECT). All checks below were performed via **source-code inspection** of the cloned repo. Live HTTP status codes, rendered HTML, and redirect chain verification could not be performed from this environment. GSC / browser verification recommended for items marked `⚠️ VERIFY LIVE`.

---

## Check 1 — robots.txt 🟢 PASS

Source: `app/robots.ts` (Next.js `MetadataRoute.Robots`)

| Item | Expected | Found in source | Status |
|---|---|---|---|
| `Allow: /_next/static/` | yes | `commonAllow` array includes `'/_next/static/'` | ✅ |
| `Allow: /_next/image` | yes | `commonAllow` includes `'/_next/image'` and `'/_next/image/'` | ✅ |
| `Sitemap: https://www.manuelsolis.com/sitemap.xml` | yes | `sitemap: \`${SITE_URL}/sitemap.xml\`` | ✅ |
| GPTBot blocked | yes | `{ userAgent: 'GPTBot', disallow: '/' }` | ✅ |
| ClaudeBot blocked | yes | `{ userAgent: 'ClaudeBot', disallow: '/' }` | ✅ |
| anthropic-ai blocked | yes | `{ userAgent: 'anthropic-ai', disallow: '/' }` | ✅ |
| CCBot blocked | yes | `{ userAgent: 'CCBot', disallow: '/' }` | ✅ |

**Note:** Search/answer bots (OAI-SearchBot, ChatGPT-User, Claude-SearchBot, Claude-User, PerplexityBot, Perplexity-User, Applebot-Extended) are explicitly **allowed** — this is intentional per in-code comments distinguishing training crawlers (blocked) from query-answering bots (allowed). No change needed.

Host-awareness logic is intact: staging/preview hosts (`v2.`, `*.vercel.app`, `bos.`, `mme.`) receive `Disallow: /` with no sitemap.

⚠️ VERIFY LIVE: Confirm rendered output at `https://www.manuelsolis.com/robots.txt` matches source intent.

---

## Check 2 — Sitemap Index + 7 Shards 🟢 PASS (source)

All sitemap files are Next.js route handlers (`app/sitemap*.xml/route.ts`), generating XML at request time from TypeScript data.

**Index (`/sitemap.xml`):** References exactly 7 shards — `sitemap-pages.xml`, `sitemap-servicios.xml`, `sitemap-oficinas.xml`, `sitemap-abogados.xml`, `sitemap-landings.xml`, `sitemap-blog.xml`, `sitemap-newsletter.xml` ✅

**Shard URL count estimates** (derived from source data × 2 langs):

| Shard | Source data | Estimated URLs | Expected range | Status |
|---|---|---|---|---|
| sitemap-pages.xml | ≥15 static pages + category pages, ×2 langs | ~32–44 | 30–60 | ✅ |
| sitemap-servicios.xml | 11 services + ~8 non-virtual accident offices, ×2 langs | ~38–46 | 30–60 | ✅ |
| sitemap-oficinas.xml | 21 offices + 1 hub, ×2 langs | ~44 | 30–60 | ✅ |
| sitemap-abogados.xml | 20 attorneys ×2 langs | 40 | 30–60 | ✅ |
| sitemap-landings.xml | ~25+ city×service landings ×2 langs | ~50–84 | 30–60* | ⚠️ WARN |
| sitemap-blog.xml | Published posts + 1 hub, ×2 langs | ~44+ | 30–60 | ✅ |
| sitemap-newsletter.xml | 5 editions + 1 hub, ×2 langs | 12 | 30–60* | ⚠️ WARN |

*`sitemap-landings.xml` legitimately exceeds 60 as the landing inventory grows (each city×service combination is a separate URL). `sitemap-newsletter.xml` at 12 is below the flag threshold; this is expected since only 5 newsletter editions are published. Neither warrants code action — both reflect actual content volume.

**Source code structure:** All shards use `publishable()` guard (filters out URLs with a matching 301 redirect), `xhtml:link` alternates in each `<url>` entry, and `shardLastmod()` derived from real content dates — correct per DISCOVERY_v3 spec.

⚠️ VERIFY LIVE: Confirm all 8 endpoints return HTTP 200 + valid XML via GSC Sitemaps tab.

---

## Check 3 — Legacy URL Redirects 🟢 PASS (source)

Source: `app/lib/seoRedirects.ts` (285 `permanent: true` entries)

All 8 audit test URLs have matching redirect patterns. Destinations are correct; note that unprefixed sources correctly prepend `/es/` per the in-code rule ("si el source NO tiene :lang, el destination debe llevar /es explícito").

| Test URL | Pattern in source | Declared destination | Expected | Status |
|---|---|---|---|---|
| `/preguntas-frecuentes/algo` | `/preguntas-frecuentes/:slug*` | `/es/informacion/faq` | `/informacion/faq` → `/es/informacion/faq` ✅ | ✅ |
| `/noticias/cualquier-cosa` | `/noticias/:slug*` | `/es/blog` | `/blog` → `/es/blog` ✅ | ✅ |
| `/areas-servicio/asilo` | `/areas-servicio/asilo` | `/es/servicios/asilo` | `/servicios/asilo` ✅ | ✅ |
| `/oficinas/houston` | `/oficinas/houston` | `/es/oficinas/houston-principal` | `/oficinas/houston-principal` ✅ | ✅ |
| `/landing-google-immigration` | `/:path(landing-google-.+)` | `/es` | `/` (= `/es`) ✅ | ✅ |
| `/proteccion-legal-para-migrantes/test` | `/proteccion-legal-para-migrantes/:slug*` | `/es/blog` | `/blog` ✅ | ✅ |
| `/oficinas/los-angeles` | `/oficinas/los-angeles` | `/es/oficinas/losangeles` | `/oficinas/losangeles` ✅ | ✅ |
| `/politica-de-privacidad` | `/politica-de-privacidad` | `/es/privacidad` | `/privacidad` ✅ | ✅ |

No 404 destinations found in source. All redirect entries carry `permanent: true` (301).

⚠️ VERIFY LIVE: Run redirect chains in browser / GSC URL inspection to confirm actual HTTP 301→200 for each.

---

## Check 4 — "Crawled – not indexed" Pages Content Quality 🟢 PASS (source)

Pages audited via source code:

### `/en/abogado-accidentes-dallas` & `/es/abogado-accidentes-houston`

Both use `CityServiceLanding` component and follow the same template. Evidence from page source files:

| Check | Evidence | Status |
|---|---|---|
| FAQPage JSON-LD schema | `'@type': 'FAQPage'` at line 73 of each page.tsx | ✅ |
| BreadcrumbList JSON-LD | `generateBreadcrumbSchema()` called, output in `<script id="breadcrumb-schema">` | ✅ |
| `<details>` tags present | `CityServiceLanding` component (`app/components/CityServiceLanding.tsx:406`) renders native `<details>` for FAQ accordion | ✅ |
| 'Casos típicos'/'Typical Cases' string | `CityServiceLanding.tsx:367` — `{isEs ? 'Casos típicos' : 'Typical Cases'}` | ✅ |
| Canonical matches lang/slug | `alternates.canonical: \`${SITE_URL}/${lang}/${PAGE_SLUG}\`` | ✅ |
| hreflang es/en/x-default | `alternates.languages` block with es, en, x-default present | ✅ |

### `/en/abogados/manuel-solis-iii`

| Check | Evidence | Status |
|---|---|---|
| Person JSON-LD schema (not FAQPage) | `'@type': 'Person'` at `[slug]/page.tsx:84` | ✅ |
| BreadcrumbList JSON-LD | `generateBreadcrumbSchema()` called at line 128, rendered as `<script>` | ✅ |
| Canonical matches lang/slug | `alternates.canonical: \`${SITE_URL}/${lang}/abogados/${slug}\`` | ✅ |
| hreflang es/en/x-default | Attorney page uses `alternates.languages` pattern confirmed present in page file | ✅ |

⚠️ VERIFY LIVE: Use Googlebot UA (`Googlebot/2.1`) to fetch each page and confirm no 'Application error' markers; paste JSON-LD into Google's Rich Results Test.

---

## Check 5 — hreflang Sanity on Key Pages 🟢 PASS (source)

| Page | Canonical | es | en | x-default | Status |
|---|---|---|---|---|---|
| `/es/servicios/inmigracion` | `${SITE_URL}/${lang}/servicios/inmigracion` | `${SITE_URL}/es/servicios/inmigracion` | `${SITE_URL}/en/servicios/inmigracion` | `${SITE_URL}/es/...` | ✅ |
| `/en/servicios/inmigracion` | same template | ✅ | ✅ | ✅ | ✅ |
| `/es/oficinas/houston-principal` | `https://www.manuelsolis.com/${lang}/oficinas/houston-principal` | present | present | present | ✅ |
| `/es/blog/tps-2026-paises-elegibles-renovacion` | `${SITE_URL}/${lang}/blog/tps-2026-paises-elegibles-renovacion` | `${SITE_URL}/es/blog/...` | `${SITE_URL}/en/blog/...` | `${SITE_URL}/es/...` | ✅ |

All four pages: canonical uses lang-aware URL (`/${lang}/...`), hreflang languages object includes es, en, and x-default pointing to the /es variant. No mismatches in source.

**Sitemap xhtml:link alternates:** `sitemapData.ts:69–71` correctly emits `hreflang="es"`, `hreflang="en"`, `hreflang="x-default"` (→ /es) for every bilingual URL entry.

⚠️ VERIFY LIVE: GSC → Search Results → International Targeting → Language report; confirm no hreflang errors.

---

## Check 6 — No Regression in Core Flows 🟢 PASS (source)

| URL | Source behavior | Status |
|---|---|---|
| `https://www.manuelsolis.com/` | `app/page.tsx:6` — `redirect('/es')` | ✅ redirect to /es |
| `https://www.manuelsolis.com/es` | `app/[lang]/page.tsx` — renders home for `lang=es` | ✅ |
| `https://www.manuelsolis.com/en` | same template, `lang=en` | ✅ |
| `https://www.manuelsolis.com/es/abogados` | `app/[lang]/abogados/page.tsx` — canonical `/es/abogados` | ✅ |
| `https://www.manuelsolis.com/es/oficinas` | `app/[lang]/oficinas/page.tsx` — canonical `/es/oficinas` | ✅ |

No noindex flags, no redirects, no notFound() calls on any of these routes in source.

⚠️ VERIFY LIVE: Spot-check each URL in browser for 200 OK and no "Application error" banner.

---

## Advisory — Colaboradores Section Hidden Today 🟡 WATCH

**Commit:** `ae55edc feat(colaboradores): ocultar la seccion sin romper los enlaces repartidos` (HEAD as of audit)

**What changed:** The colaboradores section is now hidden and those pages emit `noindex`. `sitemapData.ts` was updated today (comment: "Colaboradores fuera del sitemap (2026-09-01)") to remove colaborador URLs from `sitemap-pages.xml`. URLs remain live and respond with 200.

**Why to watch:** Any colaboradores URLs that were previously crawled and indexed will now be de-indexed over time (Google respects noindex on 200 responses). This is the correct approach. However:
- GSC may briefly show a spike in "Excluded — noindex" in the Coverage report.
- Those URLs will drop from the index over the next 2–8 weeks as Googlebot revisits them.
- No code action required — but Carlos should monitor GSC Coverage for unexpected scope.

---

## Punch List

### Action items for Carlos in GSC (manual)

1. **Verify live redirects:** Use the URL Inspection tool on the 8 test URLs from Check 3 to confirm actual 301→200 chains (proxy blocked live curl from this audit environment).
2. **Verify sitemap HTTP status:** GSC → Sitemaps — confirm all 8 sitemaps show "Success" with non-zero URL counts. Pay attention to `sitemap-newsletter.xml` (expected ~12 URLs) and `sitemap-landings.xml` (may exceed 60 — that's OK, flag only if 0 or error).
3. **Monitor colaboradores de-indexing:** GSC → Coverage → Excluded — "Excluded by 'noindex' tag" count may rise as Googlebot re-crawls those pages over the next few weeks. This is expected behavior.
4. **hreflang report:** GSC → International Targeting — confirm no hreflang errors, especially for newly added Chicago office pages (added 2026-08-11) and new WAVA/asilo landing pages (added 2026-08-12).
5. **Request indexing for new pages:** If any of the landings added 2026-08-12 or Chicago offices added 2026-08-11 are not yet indexed, request indexing via URL Inspection.
6. **Check coverage delta:** Review Coverage report for any new "Crawled – currently not indexed" URLs compared to the last audit cycle.

### Action items for code

*None.* No broken redirects, malformed schemas, or structural issues found in source code. The site is healthy.

### Things working as expected

- robots.txt: all AI training bots blocked, query bots allowed, Next.js static assets allowed, staging hosts get blanket Disallow.
- Sitemap architecture: 7-shard split correctly implemented; xhtml:link alternates in every URL entry; `publishable()` guard prevents 301-redirect URLs from appearing as `<loc>` entries; lastmod derived from real content dates.
- 285 permanent (301) redirect entries covering all test URL patterns with correct /es-prefixed destinations.
- FAQPage schema on all city×service landings via `CityServiceLanding` template.
- Person schema (not FAQPage) on attorney profile pages.
- BreadcrumbList schema on all checked pages.
- `<details>` accordion + "Casos típicos"/"Typical Cases" sections on landing pages.
- Canonical + hreflang (es/en/x-default) correct on all checked pages.
- Root `/` → `/es` redirect in place.
- Colaboradores noindex + sitemap removal handled cleanly in one commit (no lingering sitemap entries for noindex pages).

---

*Audit performed via source-code inspection (live site unreachable from execution environment). Next audit: ~2026-09-15.*
