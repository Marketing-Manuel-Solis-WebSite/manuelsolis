# SEO Audit — manuelsolis.com · 2026-08-15

**Repo:** `manuelsolislawoffice/manuelsolis` · **Branch:** `main`  
**Auditor:** Automated biweekly audit · **Scope:** Static source-code analysis  
**Important:** Network egress policy blocked all outbound requests to `www.manuelsolis.com:443` from this CI environment — every check below was performed against the source code in this repository, not against the live site. HTTP status codes and live-rendered HTML could not be verified. Carlos should validate live HTTP independently (browser or GSC).

---

## Check 1 — robots.txt

**PASS ✅**

Source: `app/robots.ts` (Next.js `MetadataRoute.Robots`)

| Requirement | Status | Evidence |
|---|---|---|
| `Allow: /_next/static/` | ✅ | `commonAllow` array (line 33) |
| `Allow: /_next/image` | ✅ | `commonAllow` array (line 33) |
| `Sitemap: https://www.manuelsolis.com/sitemap.xml` | ✅ | `sitemap: \`${SITE_URL}/sitemap.xml\`` (line 129) |
| `GPTBot` blocked | ✅ | `{ userAgent: 'GPTBot', disallow: '/' }` (line 124) |
| `ClaudeBot` blocked | ✅ | `{ userAgent: 'ClaudeBot', disallow: '/' }` (line 127) |
| `anthropic-ai` blocked | ✅ | `{ userAgent: 'anthropic-ai', disallow: '/' }` (line 126) |
| `CCBot` blocked | ✅ | `{ userAgent: 'CCBot', disallow: '/' }` (line 125) |

Notes:
- AI *query* bots (OAI-SearchBot, ChatGPT-User, Claude-SearchBot, Claude-User, PerplexityBot) remain explicitly **allowed** — this is intentional per code comments (citation bots, not training scrapers).
- Non-primary hosts (v2./, staging, Vercel preview deployments) receive a blanket `Disallow: /` — still correct.

---

## Check 2 — Sitemap index + 7 shards

**PASS ✅ (source-verified; live HTTP unverifiable)**

Source: `app/sitemap.xml/route.ts` (index) + 7 shard route handlers.

### Sitemap index
- Returns a `<sitemapindex>` (built by `buildSitemapIndexXml`) referencing **exactly 7 shards**.
- All 7 shard `<loc>` entries point to `https://www.manuelsolis.com/sitemap-*.xml`.
- Each shard `<lastmod>` is the real `max(lastModified)` of its URLs — not the build timestamp.

### Shard health (estimated URL counts from source data)

| Shard | Implementation | Estimated URLs | Status |
|---|---|---|---|
| `sitemap-pages.xml` | `getPagesEntries()` | ~42 (18 static routes × 2 langs + 3 collaborators × 2) | ✅ |
| `sitemap-servicios.xml` | `getServiciosEntries()` | ~55+ (11 services + ~16 accident-office sub-pages, minus virtual, × 2) | ✅ |
| `sitemap-oficinas.xml` | `getOficinasEntries()` | ~42 (20 office slugs + hub × 2) | ✅ |
| `sitemap-abogados.xml` | `getAbogadosEntries()` | ~40 (20 attorneys × 2) | ✅ |
| `sitemap-landings.xml` | `getLandingsEntries()` | ~70 (35 city×service landings × 2) | ✅ |
| `sitemap-blog.xml` | `getBlogEntries()` | ~114 (56 posts + hub × 2) | ✅ |
| `sitemap-newsletter.xml` | `getNewsletterEntries()` | ~8 (3 editions + hub × 2) | ✅ |

Notable recent changes reflected in sitemap data:
- **2026-08-12:** 10 new VAWA/asilo landings added with correct `lastmod: '2026-08-12'` (not stale).
- **2026-08-11:** 5 new Chicago virtual office pages (`chicago-martingale`, `chicago-prospect`, `chicago-wacker`, `chicago-burr-ridge`, `chicago-wall`) with correct `lastmod: '2026-08-11'`.

GSC action: Confirm all shards show HTTP 200 in GSC sitemap submission status. The Chicago virtual offices carry `noindex` at page level — they should appear in `sitemap-oficinas.xml` but GSC will report them as "Excluded (noindex)". This is expected; remove them from the sitemap if GSC coverage errors pile up.

---

## Check 3 — Legacy URL redirect rules (301)

**PASS ✅ (source-verified; live HTTP chain unverifiable)**

Source: `app/lib/seoRedirects.ts` — **285 total redirect rules** (up from 266 at last count; 19 new rules added 2026-08-10 covering real 404s from production logs).

| Test URL | Expected destination | Rule found | Status |
|---|---|---|---|
| `/preguntas-frecuentes/algo` | `/informacion/faq` | `/preguntas-frecuentes/:slug*` → `/es/informacion/faq` (line 194) | ✅ |
| `/noticias/cualquier-cosa` | `/blog` | `/noticias/:slug*` → `/es/blog` (line 206) | ✅ |
| `/areas-servicio/asilo` | `/servicios/asilo` | `/areas-servicio/asilo` → `/es/servicios/asilo` (line 258) | ✅ |
| `/oficinas/houston` | `/oficinas/houston-principal` | `/oficinas/houston` → `/es/oficinas/houston-principal` (line 360) | ✅ |
| `/landing-google-immigration` | `/` (home) | `/:path(landing-google-.+)` → `/es` (line 433) | ✅ |
| `/proteccion-legal-para-migrantes/test` | `/blog` | `/proteccion-legal-para-migrantes/:slug*` → `/es/blog` (line 216) | ✅ |
| `/oficinas/los-angeles` | `/oficinas/losangeles` | `/oficinas/los-angeles` → `/es/oficinas/losangeles` (line 366) | ✅ |
| `/politica-de-privacidad` | `/privacidad` | `/politica-de-privacidad` → `/es/privacidad` (line 411) | ✅ |

All redirect destinations are locale-prefixed (`/es/...`), avoiding double-301 chains when the locale middleware fires.

---

## Check 4 — "Crawled – not indexed" pages content quality

**PASS ✅ (source-verified)**

### `/en/abogado-accidentes-dallas` (city×service landing)

| Signal | Status | Evidence |
|---|---|---|
| FAQPage JSON-LD | ✅ | `faqPageSchema` built from `getLocalFAQ()`, emitted as `<script type="application/ld+json" id="faq-schema">` (page.tsx line 85-87) |
| BreadcrumbList JSON-LD | ✅ | `breadcrumbData` via `generateBreadcrumbSchema()`, emitted as `<script id="breadcrumb-schema">` (page.tsx line 83) |
| `<details>` tag | ✅ | Native `<details>` in FAQ accordion, `CityServiceLanding.tsx` line 406 |
| "Typical Cases" heading | ✅ | `{isEs ? 'Casos típicos' : 'Typical Cases'}` — `CityServiceLanding.tsx` line 367 |
| `<link rel="canonical">` | ✅ | `canonical: \`${SITE_URL}/${lang}/${PAGE_SLUG}\`` (page.tsx line 30) |
| hreflang es/en/x-default | ✅ | `languages: { es, en, 'x-default': es }` (page.tsx lines 31-34) |
| 200 OK / no error markers | ⚠️ | Live HTTP unverifiable (egress blocked) |

### `/en/abogados/manuel-solis-iii` (attorney profile)

| Signal | Status | Evidence |
|---|---|---|
| Person JSON-LD (not FAQPage) | ✅ | `getPersonSchema()` returns `@type: 'Person'` with `name`, `jobTitle`, `image`, `worksFor`, `alumniOf`, `workLocation` (page.tsx lines 75-122) |
| BreadcrumbList JSON-LD | ✅ | `getBreadcrumbSchema()` emitted alongside Person schema (page.tsx lines 126-133, 154-157) |
| `<link rel="canonical">` | ✅ | `canonical: \`${SITE_URL}/${lang}/abogados/${slug}\`` (page.tsx line 57) |
| hreflang es/en/x-default | ✅ | `languages: { es, en, 'x-default': es }` (page.tsx lines 58-62) |
| 200 OK / no error markers | ⚠️ | Live HTTP unverifiable (egress blocked) |

### `/es/abogado-accidentes-houston` (city×service landing)

Identical page structure to the Dallas landing (same `page.tsx` template, different `PAGE_SLUG = 'abogado-accidentes-houston'`). All five signals carry through:

| Signal | Status |
|---|---|
| FAQPage JSON-LD | ✅ |
| BreadcrumbList JSON-LD | ✅ |
| `<details>` tag | ✅ |
| "Casos típicos" heading | ✅ |
| canonical + hreflang es/en/x-default | ✅ |
| 200 OK / no error markers | ⚠️ Live HTTP unverifiable |

---

## Check 5 — hreflang sanity on key pages

**PASS ✅ (source-verified)**

All pages use Next.js `generateMetadata` → `alternates.languages` → rendered as `<link rel="alternate" hreflang>` tags by the framework.

| Page | canonical | es | en | x-default | Status |
|---|---|---|---|---|---|
| `/es/servicios/inmigracion` | `/${lang}/servicios/inmigracion` | `/es/servicios/inmigracion` | `/en/servicios/inmigracion` | `/es/servicios/inmigracion` | ✅ |
| `/en/servicios/inmigracion` | same pattern, `lang=en` canonical | ✅ | ✅ | `/es/...` | ✅ |
| `/es/oficinas/houston-principal` | `/${lang}/oficinas/houston-principal` | `/es/oficinas/houston-principal` | `/en/oficinas/houston-principal` | `/es/oficinas/houston-principal` | ✅ |
| `/es/blog/tps-2026-paises-elegibles-renovacion` | `/${lang}/blog/tps-2026-...` | ✅ | ✅ | `/es/blog/tps-2026-...` | ✅ |

The `buildSitemapXml()` function in `sitemapData.ts` additionally injects `<xhtml:link rel="alternate" hreflang>` pairs inside each `<url>` block, reinforcing hreflang at the sitemap level.

---

## Check 6 — No regression in core flows

**UNVERIFIABLE ⚠️ (egress blocked)**

All five URLs could not be fetched from the CI environment. Carlos should open each in a browser and confirm:

| URL | Expected | Live check |
|---|---|---|
| `https://www.manuelsolis.com/` | 301 → `/es` → 200 | Not checked |
| `https://www.manuelsolis.com/es` | 200 | Not checked |
| `https://www.manuelsolis.com/en` | 200 | Not checked |
| `https://www.manuelsolis.com/es/abogados` | 200 | Not checked |
| `https://www.manuelsolis.com/es/oficinas` | 200 | Not checked |

No code changes have been made to routing, layout, or proxy middleware since the last audit. The `proxy.ts` locale-detection and the `/es` default behaviour are unchanged.

---

## Overall Status: 🟡 YELLOW

The source code is clean and structurally sound on every dimension. Yellow is assigned solely because live HTTP could not be confirmed — the CI network policy blocks `www.manuelsolis.com:443`. If Carlos verifies core flows are live (Check 6), this audit upgrades to 🟢 GREEN with no action items in code.

---

## Punch List

### Action items for Carlos in GSC (manual)

1. **Verify live HTTP status** — open the 5 core flow URLs from Check 6 in a browser and confirm 200 / no error page.
2. **Check GSC sitemap submission** — confirm all 7 shards show HTTP 200 and are processing without errors. Last new shards were submitted after 2026-08-11 (Chicago offices) and 2026-08-12 (10 VAWA/asilo landings).
3. **GSC Coverage report** — look for the 10 new landings (`vawa-*`, `asilo-politico-*`) added 2026-08-12 moving from "Crawled – not indexed" to "Indexed." These pages now have proper city-localized FAQPage schema, `<details>` FAQ, and Typical Cases sections; GSC should start indexing them.
4. **Chicago virtual offices** — `chicago-martingale`, `chicago-prospect`, `chicago-wacker`, `chicago-burr-ridge`, `chicago-wall` carry page-level `noindex` (virtual/appointment-only addresses). They will appear in GSC Coverage as "Excluded (noindex)." This is intentional; if GSC flags them as errors, remove them from `sitemap-oficinas.xml` by adding their slugs to an exclusion filter.
5. **Request indexing** for the 10 new landings and 5 Chicago office pages via GSC URL Inspection → "Request indexing" if they haven't been crawled yet.
6. **Monitor "Crawled – not indexed" count** — the 2026-08-12 content expansion (VAWA/asilo/defensa city matrix) was specifically designed to address this GSC coverage error. Track in the next 2–4 weeks.

### Action items for code

*None.* No live breakage detected in source. The site code is correct.

### Things working as expected

- robots.txt: all 4 AI training bots blocked, Next.js static paths allowed, sitemap link correct.
- Sitemap: 7-shard split intact, all route handlers present, `lastmod` derived from real content dates (not build time), virtual-office and redirected-URL exclusion logic in place.
- 285 redirect rules covering all legacy WordPress paths, defunct attorney slugs, slug normalizations, and real 404s from production logs (last batch added 2026-08-10).
- FAQPage schema present on all city×service landings (conditioned on `localFAQ.length > 0`).
- Person schema (not FAQPage) on all attorney profile pages; BreadcrumbList on both.
- hreflang `es / en / x-default → es` consistently applied across services, offices, blog, and landings.
- Sitemaps include `<xhtml:link rel="alternate">` pairs for additional hreflang reinforcement.
- 10 VAWA/asilo/defensa landings added 2026-08-12 with proper city-localized FAQ content and correct lastmod.
- 5 Chicago virtual office pages added 2026-08-11 with proper lastmod and noindex on page.

---

*Automated audit · Manuel Solis Law Firm · Generated 2026-08-15*
