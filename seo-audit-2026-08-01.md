# SEO Health Audit — 2026-08-01

**Site:** manuelsolis.com (Next.js 16 on Vercel, Manuel Solís Law Firm)
**Audited by:** Automated biweekly routine
**Overall status:** 🟡 YELLOW

> All source-code checks pass. Live HTTP verification was blocked by the execution
> environment's egress proxy policy (403 to `manuelsolis.com:443`). No actionable
> code issues were found. Yellow reflects the inability to confirm live behavior,
> not any known problem.

---

## Verification method note

`curl` and `WebFetch` calls to `manuelsolis.com` returned HTTP 403 (connect\_rejected)
from the org egress proxy. All checks below are therefore based on **static source-code
analysis** of the deployed repository (`HEAD` = `81aa67a`). Live HTTP status codes,
redirect chains, and rendered HTML could not be confirmed. Google Search Console
remains the ground truth for live crawl behavior.

---

## CHECK 1 — robots.txt

**Result: ✅ PASS (source-verified)**

File: `app/robots.ts`

| Item | Expected | Found |
|------|----------|-------|
| `Allow: /_next/static/` | ✅ | `commonAllow` array, line 33 |
| `Allow: /_next/image` | ✅ | `commonAllow` array, line 33 |
| `Allow: /_next/image/` | ✅ | `commonAllow` array, line 33 |
| `Sitemap: https://www.manuelsolis.com/sitemap.xml` | ✅ | line 100 |
| `GPTBot Disallow: /` | ✅ | line 95 |
| `CCBot Disallow: /` | ✅ | line 96 |
| `anthropic-ai Disallow: /` | ✅ | line 97 |
| `ClaudeBot Disallow: /` | ✅ | line 98 |

Host-aware logic is present: non-primary hosts (v2.*, *.vercel.app, bos.*, mme.*)
receive a full `Disallow: /` with no sitemap link (lines 59–63).

---

## CHECK 2 — Sitemap index + 7 shards

**Result: ✅ PASS (source-verified) | ⚠️ WARN: live HTTP unconfirmed**

File: `app/sitemap.xml/route.ts` → `app/lib/sitemapData.ts`

The sitemap index is a `<sitemapindex>` referencing exactly 7 shards (lines 18–25).
All shards are wired to data-driven entry generators; estimated URL counts from source data:

| Shard | Data source | Est. `<url>` count | Expected range |
|-------|-------------|---------------------|----------------|
| sitemap-pages.xml | 18 static routes + 3 collaborators × 2 langs | ~42 | 30–60 ✅ |
| sitemap-servicios.xml | 11 service pages + 16 accident offices × 2 langs | ~54 | 30–60 ✅ |
| sitemap-oficinas.xml | 1 hub + 15 offices × 2 langs | 32 | 30–60 ✅ |
| sitemap-abogados.xml | 20 attorneys × 2 langs | 40 | 30–60 ✅ |
| sitemap-landings.xml | 26 city-service landings × 2 langs | 52 | 30–60 ✅ |
| sitemap-blog.xml | 35 entries (hub + 34 posts) × 2 langs | 70 | >30 ✅ |
| sitemap-newsletter.xml | 1 hub + 3 editions × 2 langs | 8 | >0 ✅ |

All shards generate > 0 URLs. `buildSitemapXml` includes `xhtml:link` hreflang
alternates (es / en / x-default) for every entry (sitemapData.ts lines 38–47).

**⚠️ Live HTTP status (200, valid XML) could not be verified.** Carlos should confirm
all 8 sitemaps show green in GSC > Sitemaps.

---

## CHECK 3 — Legacy URL redirects (301 verification)

**Result: ✅ PASS (source-verified) | ⚠️ WARN: live redirect chains unconfirmed**

File: `app/lib/seoRedirects.ts` → integrated in `next.config.ts` line 57

Redirect count: **268** `permanent: true` entries (task context says 266 — 2 additions
since last documentation; not a problem).

All 8 test cases found in source with correct `permanent: true` destinations:

| Test URL | Expected destination | Source line | Status |
|----------|---------------------|-------------|--------|
| `/preguntas-frecuentes/algo` | `/es/informacion/faq` | L146–148 | ✅ |
| `/noticias/cualquier-cosa` | `/es/blog` | L158 | ✅ |
| `/areas-servicio/asilo` | `/es/servicios/asilo` | L210 | ✅ |
| `/oficinas/houston` | `/es/oficinas/houston-principal` | L312 | ✅ |
| `/landing-google-immigration` | `/es` | L385 (regex match) | ✅ |
| `/proteccion-legal-para-migrantes/test` | `/es/blog` | L168 | ✅ |
| `/oficinas/los-angeles` | `/es/oficinas/losangeles` | L318 | ✅ |
| `/politica-de-privacidad` | `/es/privacidad` | L363 | ✅ |

No 404 destinations found. All non-prefixed sources correctly point to `/es/...` targets
(avoiding a chained second 301 from locale injection).

---

## CHECK 4 — "Crawled - not indexed" page content quality

**Result: ✅ PASS (source-verified)**

### `/en/abogado-accidentes-dallas` and `/es/abogado-accidentes-houston`
Files: `app/[lang]/abogado-accidentes-dallas/page.tsx`, `app/[lang]/abogado-accidentes-houston/page.tsx`

Both use the shared `CityServiceLanding` component with identical schema pipeline:

| Criterion | Check | Result |
|-----------|-------|--------|
| (a) FAQPage JSON-LD schema | `faqPageSchema` with `"@type": "FAQPage"` (page.tsx L75–83) | ✅ |
| (a) FAQPage conditional on `localFAQ.length > 0` | City signals exist for `houston` and `dallas` in `CITY_LOCAL` | ✅ |
| (b) BreadcrumbList JSON-LD | `generateBreadcrumbSchema()` injected (page.tsx L59–63) | ✅ |
| (c) `<details>` tag | Native `<details>` in `CityServiceLanding.tsx` L365 | ✅ |
| (c) 'Casos típicos' / 'Typical Cases' | Rendered at `CityServiceLanding.tsx` L326 | ✅ |
| (d) `<link rel="canonical">` matches lang/slug | `alternates.canonical` = `${SITE_URL}/${lang}/${PAGE_SLUG}` | ✅ |
| (d) hreflang es/en/x-default | `alternates.languages` includes es, en, x-default | ✅ |
| (e) HTTP 200 / no error markers | **BLOCKED** — live check unavailable | ⚠️ |

### `/en/abogados/manuel-solis-iii`
File: `app/[lang]/abogados/[slug]/page.tsx`

| Criterion | Check | Result |
|-----------|-------|--------|
| (a) Person schema (not FAQPage) | `@type: "Person"` with `worksFor`, `alumniOf`, `jobTitle` (L60–103) | ✅ |
| (b) BreadcrumbList JSON-LD | `generateBreadcrumbSchema()` (L106–113) | ✅ |
| (d) canonical + hreflang es/en/x-default | `alternates` in `generateMetadata()` (L41–47) | ✅ |
| (e) HTTP 200 / no error markers | **BLOCKED** — live check unavailable | ⚠️ |

---

## CHECK 5 — hreflang sanity on key pages

**Result: ✅ PASS (source-verified)**

All four pages have `canonical` + `languages` (es / en / x-default) configured in `generateMetadata()`:

| Page | Canonical pattern | hreflang | Source file |
|------|-------------------|----------|-------------|
| `/es/servicios/inmigracion` | `${SITE_URL}/${lang}/servicios/inmigracion` | es / en / x-default→es | servicios/inmigracion/page.tsx L24–31 |
| `/en/servicios/inmigracion` | same template, `lang='en'` | es / en / x-default→es | same file |
| `/es/oficinas/houston-principal` | `https://www.manuelsolis.com/${lang}/oficinas/houston-principal` | es / en / x-default→es | houston-principal/page.tsx L40–46 |
| `/es/blog/tps-2026-paises-elegibles-renovacion` | `${SITE_URL}/${lang}/blog/tps-2026-...` | es / en / x-default→es | tps blog page.tsx L493–500 |

x-default consistently points to `/es/` variants. ✅

---

## CHECK 6 — No regression in core flows

**Result: ⚠️ WARN — live HTTP unconfirmed**

Source confirms all routes exist as Next.js app-router segments. No errors were
detected in repository code. Live 200-OK verification blocked by proxy policy.

| URL | Route exists in source | Expected behavior |
|-----|----------------------|-------------------|
| `https://www.manuelsolis.com/` | Root → middleware locale redirect to `/es` | ✅ source |
| `/es` | `app/[lang]/page.tsx` | ✅ source |
| `/en` | `app/[lang]/page.tsx` | ✅ source |
| `/es/abogados` | `app/[lang]/abogados/page.tsx` | ✅ source |
| `/es/oficinas` | `app/[lang]/oficinas/page.tsx` | ✅ source |

**Recent commits affecting non-SEO files only** (no changes to robots.ts, sitemapData.ts,
seoRedirects.ts, or any landing/attorney/service page in the last 5 commits):

```
81aa67a copy(portada): video Uniendo Familias EP.4 (recortada)
9fcf7ef feat(portada): nuevo video Uniendo Familias EP.4
f026bff fix(leads): UTMs explícitos — organic fallback
f4bf5c3 fix(leads): inyecta UTMs en payload BOS
7d74093 assets(blog): imágenes reales julio 2026
```

SEO-critical files have not been touched. ✅

---

## Punch List

### Action items for Carlos in GSC (manual)

1. **Validate live sitemap HTTP status** — In GSC → Sitemaps, confirm all 8 sitemaps
   show "Success" (not errors or warnings). The automated audit could not verify HTTP 200
   from the current execution environment.

2. **Request indexing for "Crawled - not indexed" pages** — For `/en/abogado-accidentes-dallas`,
   `/en/abogados/manuel-solis-iii`, `/es/abogado-accidentes-houston`, use the URL Inspection
   tool → "Request indexing" if GSC still shows them as crawled-not-indexed.

3. **Check Coverage report deltas** — Compare current "Crawled - currently not indexed"
   count vs. the last biweekly baseline. If landing pages are starting to index,
   that's the content quality changes (FAQPage schema, `<details>` FAQ, typical cases) working.

4. **Sitemap submission status** — Confirm sitemap-blog.xml was resubmitted after the
   July 2026 posts were added (last `lastModified` in blog shard: 2026-07-03).

### Action items for code

**None.** No broken redirects, missing schemas, or malformed JSON-LD found in source.
The redirect count has grown from 266 to 268 entries — this is normal organic growth,
not a problem.

### Things working as expected

- ✅ robots.txt: all AI bot blocks in place; `/_next/static/` and `/_next/image` allowed;
  sitemap reference correct; non-primary host logic blocks staging/preview domains.
- ✅ Sitemap architecture: clean 7-shard split; data-driven entry generators (new offices,
  attorneys, landings added automatically); `xhtml:link` hreflang alternates in each `<url>`.
- ✅ All 8 legacy redirect test cases point to correct 301 destinations with no double-301 chains.
- ✅ City-service landing pages: FAQPage + BreadcrumbList + LegalService schemas all present;
  native `<details>` FAQ and "Casos típicos" sections wired up; canonical + hreflang es/en/x-default correct.
- ✅ Attorney profile page: Person schema (not FAQPage) with `worksFor`, `alumniOf`, `jobTitle`;
  BreadcrumbList; canonical + hreflang correct.
- ✅ All four hreflang spot-check pages confirm consistent `x-default → /es/` pattern.
- ✅ No SEO-critical files modified in the 5 most recent commits.
